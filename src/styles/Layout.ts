/**
 * Keep phone-oriented content readable when the app runs in iPad
 * compatibility mode or in a resizable iPad window.
 */
export const CONTENT_MAX_WIDTH = 720;
export const FORM_MAX_WIDTH = 480;
export const MODAL_MAX_WIDTH = 360;
